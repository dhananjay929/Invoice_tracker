Options -Indexes

# Pass Authorization header to PHP (critical fix)
SetEnvIf Authorization "(.*)" HTTP_AUTHORIZATION=$1
RewriteEngine On
RewriteCond %{HTTP:Authorization} ^(.*)
RewriteRule .* - [e=HTTP_AUTHORIZATION:%1]

<FilesMatch "\.(env|json|md|sql)$">
    Require all denied
</FilesMatch>